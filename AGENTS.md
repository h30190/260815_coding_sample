# ArchClock 專案開發與 AI Agent 運作規範 (AGENTS.md)

本文件整理了本專案的架構設計、程式碼規範以及 AI 助理的運作原則，以確保後續開發的一致性與品質。

---

## 1. 專案架構與技術棧

- **核心技術**：React 19 + TypeScript (TSX) + Vite 6
- **樣式與設計**：Tailwind CSS + motion/react (Framer Motion) + Lucide React 圖標庫
- **資料儲存機制**：**完全本地化 (Local Standalone)**
  - 專案已移除所有 Google Firebase 雲端網路連線。
  - 所有資料持久化（包括使用者設定、打卡紀錄、看板任務）統一使用瀏覽器的 `localStorage`。
  - 模擬資料庫與驗證邏輯統一集中於 `src/lib/firebase.ts`，以便後續如果要切換回真實資料庫時能無痛轉移。

---

## 2. 功能與設計規範

### 2.1 介面美學與佈局 (UI/UX)
- 保持極簡、高質感的設計語彙（使用黑、灰、白等 Neutral 中性色調，搭配卡片式陰影與精緻的邊框）。
- **動態版面調整**：
  - 打卡面板與工時統計採用舒適的 `max-w-4xl` 窄版置中佈局。
  - 看板專案管理採用 `max-w-7xl` 寬版佈局，以容納四個橫向欄位。切換分頁時須維持流暢的 Transition 動畫。

### 2.2 打卡與工時統計邏輯
- 系統自動依「年月日」為單位，將當天的打卡紀錄進行分組。
- 每組紀錄依時間排序，自動媒合成對的 `clock_in`（上班）與 `clock_out`（下班）並累計時數。
- 若使用者處於已上班但未下班的狀態，工時計算需標記「計算中」並將當前時間納入即時累計。
- 週累計工時以**週一**為每週的開始。

### 2.3 看板專案管理邏輯
- 欄位定義：`待處理 (Backlog)` -> `進行中 (In Progress)` -> `審核中 (Review)` -> `已完成 (Done)`。
- 移動機制：同時支援 HTML5 原生拖拉 (Drag & Drop) 與點擊「推進/退回」按鈕，以相容行動裝置與觸控操作。

---

## 3. AI Agent 開發與行為準則

當 AI 助理在本專案進行開發、修復或調整時，必須嚴格遵守以下準則：

- **語言限制**：一律使用**繁體中文**與使用者進行對話與說明。
- **執行安全 (CRITICAL)**：在執行任何寫入/修改代碼、安裝套件、啟動伺服器或 Git 操作前，**必須先向使用者說明並取得確認 (OK)，嚴禁未經授權自行執行指令**。
- **分支規範**：新功能開發需開立專屬分支（如 `feature/*`），測試無誤後再合併回 `main` 並清理分支。
- **專案完整性**：修改代碼時應保留既有的授權宣告 (`@license SPDX-License-Identifier: Apache-2.0`)。
